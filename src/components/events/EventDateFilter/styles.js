// @flow

import { StyleSheet } from 'react-native';
import { colors } from 'styles';

export default StyleSheet.create({
  dateText: {
    color: colors.white,
  },
  dateInput: {
    borderWidth: 0,
  },
});
