// @flow

import { StyleSheet } from 'react-native';
import { vars } from 'styles';

export default StyleSheet.create({
  container: {
  },
  surprise: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingBottom: vars.xlPadding,
  },
});
